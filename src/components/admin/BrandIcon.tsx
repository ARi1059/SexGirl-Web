import React from 'react'

// 导航栏品牌图标（admin.components.graphics.Icon），「定」字 monogram。
// 无交互 → server 组件；样式见 custom.scss .brand-icon（玫瑰底白字，圆角方块）。
export function BrandIcon() {
  return <span className="brand-icon">定</span>
}
